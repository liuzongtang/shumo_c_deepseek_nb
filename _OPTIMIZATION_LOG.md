# C 题优化日志

> 记录本仓库的优化改动：做了什么、依据是什么、实测结果、还差什么。
> 所有改动均可复现；未列出的文件一律未改动。

---

## 0. 本轮目标

冲击更高奖项。优化顺序：**P0 提交物合规 → P1 建模严谨性 → P2 模型升级 → P3 论文呈现**。
所有"改动"以"可验证"为前提：先跑对照，数字方向确认后再刷新提交文件与论文。

---

## 1. 已完成：项目诊断与 P0 核验（只读）

### 1.1 环境事实
- 本仓库 **不是 git 仓库**（无版本控制），故改前先做对照与备份（见 §6）。
- 可用 PDF 工具链：`pdftotext`（mingw64）；无 `pypdf`/`pdfplumber`（pip 无网络，SSL EOF）。
- Python 依赖：numpy 2.4.6 / pandas 3.0.3 / scipy 1.18.0 / openpyxl 3.1.5 / matplotlib 3.11.0。

### 1.2 数据与口径核实（结论：现有实现对的地方比想象的多）
| 核验项 | 结论 |
|---|---|
| 预报时点对齐 | **正确**。代码用"预报 k 小时 = 发布时刻起第 k 小时"，实证最优：6:00 发布 MAE 387.98 vs 错位假设 514.12；12:00 发布 272.03 vs 397.60 |
| 附件列约定 | 标签为**区间右端点**（`0:10`=[0:00,0:10]，`0:00+1`=24:00），`data_loader` 处理正确 |
| 附件3 结构 | 每次发布含 **24 个整点预报（跨到次日）**，0/6/12/18 齐全 |

### 1.3 论文表格 vs 提交文件同源核验
- **独立重解问题1**（`common.solve_day`，不读已有结果）：购电量 `59482.6990` kWh、购电费 `35126.9486` 元、`E0=E144=6000`，与论文表 1/表 2 六段充放电**逐项吻合**；`result1.xlsx` 144 值总量 `59482.6989`。
- **表 3/表 6 抽查**与 `result2.xlsx` / `result3.xlsx`：3/20（769.3564 / 817.3061 / 697.9973）、6/21（0.0000 / 255.2254 / 0.0000）、12/21（962.0869 / 1001.2240 / 715.4144）**逐格命中**。

### 1.4 时段标签问题的最终结论（**纠正了过程判断**）
- 过程判断（**错误**）：以为 5 个 `result*.xlsx` 的时段数据整体错位 10 分钟。
- 实测结论：附件5 模板的 144 个标签覆盖 `[0:10, 24:10]`（缺 `[0:00,0:10]`、多出 `[24:00,24:10]`）；代码 `g[(i+1) % 144]` 的填法让**每一列"标签↔数值"语义严格正确**——铁证：标签 `12:00-12:10` 列的值 = `g[72]`（真实 `[12:00,12:10]` 段），而非 `g[71]`。
- **唯一缺陷**：`g[0]`（`0:00-0:10` 购电量 1406.64 kWh）落在最后一列，该列标签为 `0:00-0:10+1`（= 24:00–24:10，物理上不存在）。
- **待决**：是否把 5 个 `result*.xlsx` 的标签改为 `0:00-0:10 … 23:50-0:00+1`、数值按行序填（低风险纯格式，待用户确认）。

---

## 2. 已完成：P1 改法1 —— 滚动层状态反馈（closed-loop）

### 2.1 问题（论文 §7.2 不足 4 自认）
原 `solve_q4.run_rolling` 在 6/12/18 时点重新优化时，初始储电量取 **预报计划轨迹** `E_plan[36]/[72]/[108]`，等价于假设"上午实际光伏 = 预报"——是"预报驱动"而非"状态反馈"。

### 2.2 落地改动
- **`common.advance_actual_soc(pv_act, load, G_seg, C_seg, D_seg, E0) -> (E_end, extra_charged)`**：新增共享函数。储能严格执行已下发的计划充放电；实际光伏超发的盈余回充储能（受功率/容量约束，其余弃光）、不足的缺口留待紧急购电兜底。
- **`solve_q4.run_rolling` 改为状态反馈版（主线）**：6/12/18 的初始 SOC 由实际光伏递推得到，中午/傍晚的真实电量由上午/下午的实际光照决定。
- **旧口径保留为 `solve_q4.run_rolling_forecast_driven`**：供对照与论文"改进前后"叙事使用。
- **`solve_rolling_mpc.py`**：A/B/C 三方案对照实验脚本（详见 §3）。

### 2.3 实测（附件4 波动电价口径）
| 方案 | 总费(元) | 调整费 | 紧急费 | 紧急购电量(kWh) | 上调天数 |
|---|---|---|---|---|---|
| 旧·预报驱动 | 15 150 992.45 | 475 841.57 | 1 588 197.24 | 446 648.50 | 292 |
| 新·状态反馈 | **15 073 388.70** | 393 564.07 | 1 592 870.99 | 448 062.13 | 272 |

- **净收益 −77 603.75 元**；机理：执行期实际盈余光伏被回充储能（全年 176.8 万 kWh），中午真实 SOC 高于计划假设 → 少按 1.5 倍"预购"（调整费 −8.2 万），代价是紧急购电微增 0.47 万。

---

## 3. 已完成：P1 改法2 —— 24 小时前瞻窗（吃满附件3 跨日预报）

- 现状：`common.build_pv_forecast_stage` 把 `t ≥ 144` 的预报全截掉，滚动窗口止于当天 24:00 —— 附件3 明给的"次日"预报未使用（论文 §7.2 不足 5 自认）。
- 改法：窗口扩到 `[t0, t0+24h)`。当天段仍与 0:00 计划比较计违约/溢价；**次日段只计纯购电费**（次日 0:00 会重新决策，其购电量不纳入报告）；终端电量锚定 0:00 预报计划轨迹在窗口末端的值，避免"为省钱掏空储能"的短视解。

### 3.1 三方案实测（附件4 口径，`_rolling_mpc_summary.txt`）
| 方案 | 总费(元) | 紧急购电量(kWh) |
|---|---|---|
| A 旧·预报驱动 / 当天窗 | **15 150 992.45** | 446 648.50 |
| B 状态反馈 / 当天窗 | **15 073 388.70** | 448 062.13 |
| C 状态反馈 + 24h 前瞻窗 | **15 073 370.05** | 448 062.13 |

- 状态反馈 **−77 603.75 元**（B−A）；24h 前瞻窗 **−18.65 元**（C−B）；合计 **−77 622.40 元**。
- **结论一**：状态反馈是实质改进（消除 §7.2 不足 4）。
- **结论二**：24h 跨日前瞻窗边际价值近乎为零（18.65 元，0.0001%），与既有结论"跨日套利约 0.4%"一致（储能仅 12 MWh、日内价差远超日间价差）。**该结论可直接作为论文对"附件3 跨日预报是否被充分利用"的定量回答**（消除 §7.2 不足 5）。
- 索引预验证：24h 窗整点索引 `k=(t0+j)//6−tau` 全年恒在 [0,23]；当天段预报与既有 `pvf` 逐点一致。

---

## 4. 已完成：主线切换与全链路重算

### 4.1 新数字（已写回提交文件）
| 口径 / 项目 | 旧 | 新 | 变化 |
|---|---|---|---|
| 问题3·附件1 同价：计划费 | 12 549 415.99 | 12 549 415.99 | — |
| 　调整费 | 444 364.53 | **356 531.06** | −87 833.47 |
| 　紧急费 | 1 567 947.09 | **1 570 969.57** | +3 022.48 |
| 　**总费** | **14 561 727.61** | **14 476 916.62** | **−84 811.00** |
| 　紧急购电量(kWh) | 426 817.00 | 427 841.51 | +1 024.51 |
| 　上调天数 | 290 | 271 | −19 |
| 问题3·附件4 波动价：**总费** | **15 150 992.45** | **15 073 388.70** | **−77 603.75** |
| 问题2（两口径） | 不变 | 不变 | 状态反馈只作用于滚动层 |

- **滚动层价值提升**（论文关键叙事）：附件1 口径"滚动相对问题2 的节省"由 **163 532.62 → 248 343.61 元**；附件4 口径由 **164 139.73 → 241 743.48 元**。
- **交叉验证**：主线 `solve_q4.run_rolling` 输出（15 073 388.70）与实验脚本 B 方案**完全一致**。
- 重算产出：`result3.xlsx`、`result4-3.xlsx`；`result2.xlsx` / `result2_deterministic.xlsx` / `result4-2.xlsx` 数值未变。

### 4.2 验证
`validate_results.py` → **38 通过 / 0 失败**（原 35 项 + 新增 3 项状态反馈递推校验：光伏为零时 SOC 与计划一致；盈余时回充且不超上限；接近满充时受容量约束）。

---

## 5. 已完成：论文刷新（数字 + 新机制说明）

### 5.1 改动内容
- **摘要**：问题三 $1456.17\to1447.69$ 万元、节省 $16.35\to24.83$ 万元；问题四滚动 $1515.10\to1507.34$ 万元、再降 $16.41\to24.17$ 万元；消融段"午间滚动价值"同步更新；并写明问题三已采用**状态反馈滚动优化（闭环 MPC）**。
- **§5.3.1**：补写状态反馈递推（$E^{0}_{k}$、$\Delta C_t$ 公式）与 24 小时前瞻窗机制；新增**假设 7**（执行期储能按计划运行、偏差由盈余回充/紧急购电吸收）。
- **§5.3.2**：结果表更新（调整费 356 531.06、紧急费 1 570 969.57、总费 **14 476 916.62**、紧急购电量 427 841.51 kWh、上调/下调 271/0）；节省额 **248 343.61 元**。
- **表 6 / 表 7**：按 `result3.xlsx` 重算值逐格更新（**表 8 未变**）。
- **§5.3.2 末**：把"是否需要引入其他时刻预报"从单一定性论证升级为**精度 + 经济价值双重论证**，新增"24h 窗口扩展的边际价值仅 18.65 元"作为定量论据。
- **§5.4.2**：滚动口径更新为 15 073 388.70 元，滚动较鲁棒再省 **241 743.48 元**。
- **§6.1**：物理不变量校验 35 项 → **38 项**（并说明新增的 3 项状态反馈校验）。
- **§6.3**：消融表"有储能·0/6/12/18 滚动" 1447.69 万元、午间滚动价值 **24.83 万元**。
- **§7.1 / §7.2**：新增"状态反馈滚动优化"优点；**删除已被解决的两条不足**（原不足 4/5），改写为"执行层实时控制律""量测噪声与状态估计"两条新方向。
- **附录 A**：补 `advance_actual_soc`、`solve_rolling_mpc.py`、`run_rolling_forecast_driven`；参考文献补一条 MPC 经典文献。

### 5.2 一致性校验（脚本化）
对论文做新旧数字集合校验：**16 个"应出现"的新数字全部命中**，**12 个"应消失"的旧数字全部清除**（含 `14 561 727.61`、`444 364.53`、`1 567 947.09`、`15 150 992.45`、`163,532.62`、`475 841.57`、`1 588 197.24`、`164,139.73`、`1456.17`、`35 项`、`16.35 万元`、`426 817.00`）。

### 5.3 产出
- `论文初稿.md`
- `论文初稿.docx` —— 由 pandoc 3.8 重新生成（2 389 826 字节），与 md 同步

### 5.4 追加：随机规划严谨化（论文 §6.5 重做）

排查出并修正**三个真实缺陷**：
1. **场景生成失真**：初版按 (月×小时) 拟合正态、再逐 10 分钟**独立抽样**，破坏误差的日内相关性 → 造出"整日持续低估光伏"的极端场景、模型过度对冲。改为**按月分层的非参数 bootstrap**（整条重采样历史误差曲线），保留日内相关性与季节结构。
2. **口径不一致**：初版在 365 天上求解、却按 334 天报告 → VSS 出现负值假象。本版把求解域与统计期统一为报告期 334 天。
3. **实现缺陷**：把功率平衡与盈余约束写进同一矩阵时，`b_ub` 与 A_ub 行号错配（1 天规模复现：目标值 84 825.78 vs 正确 71 666.91），已按行号分块修正。

**一个数学结论（重要）**：分段盈余约束 $C_t\le\max(0,G_t+PV_t\Delta t-L_t\Delta t)$ 在**联合 LP**（G 为决策变量）中因 $\max(0,\cdot)$ 使可行集非凸而**无法线性化**——直接写 $C_t\le G_t+PV_t\Delta t-L_t\Delta t$ 会丢掉 $\max(0,\cdot)$，等价于强制"每个时段都买电覆盖净负荷"，把计划费推到 1770 万元。**初版注释"无法线性化"的判断是正确的**；论文已**如实声明**：联合 LP 省略该约束 → RP 为下界、VSS 为上界估计（精确量化需场景分解/Benders）。

结果（S=10，334 天口径）：WS 1231.32、RP 1305.37、EEV 1495.70、**VSS 190.33**、**EVPI 74.04** 万元；期望紧急购电费 240.73 → 20.29 万元（**降 91.6%**）；VSS 随场景数**收敛**（216.01 / 201.30 / 190.33 万元）。较初版的"114.99 万元（非标准口径）"更严谨、数值也更大。摘要已同步；临时诊断脚本 `_diag_stoch*.py` 已删除。

---

## 6. 待办

| 优先级 | 事项 | 状态 |
|---|---|---|
| P0 | 提交物时段标签/首段修正：标签改写为 `0:00-0:10 … 23:50-0:00+1`、数值按行序；同步 `common.slot_label` / `write_slot_header` 与 `solve_q1` / `solve_q4` 写入函数，5 个文件重生成并验证语义等价（**论文表格值不变**）；回滚点 `_pre_slotfix_backup/` | ✅ 已完成 |
| P1 | 滚动层状态反馈 + 24h 前瞻窗 | ✅ 已完成 |
| P1 | 主线切换与重算（`result3.xlsx`、`result4-3.xlsx`） | ✅ 已完成 |
| P1 | 论文数字与机制刷新（md + docx） | ✅ 已完成 |
| P1 | 随机规划严谨化（bootstrap 场景 + 标准 WS/RP/EEV/VSS/EVPI + 口径局限声明） | ✅ 已完成 |
| P2 | ✅ **CVaR 风险厌恶随机规划**（论文 §6.6，Rockafellar–Uryasev 线性化，新增 `analyze_cvar.py` + [图11]）：λ=0→1 时计划费 +26.13 万、$\mathrm{CVaR}_{80\%}$ 29.48→1.26 万元（**每多付 1 元计划费压 1.08 元尾部风险**）；λ=0 结果与 §6.5 完全一致（交叉验证）；λ=0.5 处几乎免费降险 29.6%。✅ **储能循环寿命（老化）成本**（论文 §6.7，新增 `analyze_degradation.py` + [图12]）：目标加 $c_{deg}\Sigma D$；$c_{deg}$ 0→0.5 元/kWh 使储能吞吐 **−35.5%**、购电费 +58.47 万元（每 +0.1 元/kWh 约增 53~58 万元）；$c_{deg}=0$ 复现 §5.2 确定性方案 12 229 460.73 元（口径已对齐，§7.2 不足 3 相应改写）。✅ **负载不确定性（二维不确定）**（论文 §6.8，新增 `analyze_load_uncertainty.py` + [图13]）：负载按日乘性误差 $\varepsilon\sim N(0,\sigma_L)$ × 光伏 bootstrap 场景；$\sigma_L$ 0→3% 使 RP **+48.10 万元**、期望紧急费 20.60→38.47 万元（**+86.7%**）、VSS 反升至 **205.25 万元**；$\sigma_L=0$ 交叉验证 §6.5（190.34 vs 190.33 万元）。**剩余**：P3 图表/排版 | 🔶 部分完成 |
| P2 | ✅ **箱式不确定集鲁棒优化**（论文 §6.9，新增 `analyze_robust.py` + [图14]；最坏情况视角，补全"期望—风险—最坏"三视角）：因成本对光伏单调递减，箱式集最坏实现即下界 $PV^{\min}=\max(0,\widehat{PV}-\gamma\sigma)$ → 化为确定性两阶段问题（无需对偶化，严格解）。结果：**存在最优保守度** $\gamma^\ast\approx0.5$（实际光伏下总费用 $1416.12$ 万元，比点预报计划**省 56.41 万元**）；$\gamma=2$ 时最坏场景成本从 3069.65 万降到 1554.95 万元（省 1514.70 万），代价是实际多付 83.18 万元（**1 元保费对冲 18 元极端风险**） | ✅ 已完成 |
| P3 | 摘要结构精炼（21 行巨段 → **8 段**，量化结论前置）✅；**文档体检**：图引用与附录 B 清单**完全对齐（13 = 13）**、docx 内公式已转为 Word 原生 OMML（0 残留 `$`）、19→13 张图全部 ≥1500px；**中文字体定制**（theme 字体 → 中文宋体 + 英文 Times New Roman）并新增 `make_docx.py` 固化流程 ✅。**页数精简**（用 Word COM 实测迭代，原文总 34 页）：**正文 25 页、达标 ≤ 25**（总 30 页，附录另计）。做法：① 全图宽统一 60%→50%；② §5.5 多年度整节移入**附录 C**（正文留结论要点）；③ §6.2 的 3 个灵敏度表移入附录 C；④ 删除 4 张"纯数据展示型"配图（图 2/4/5/T-A，文件归档 `_unused_figures/`）并同步清理正文引用与附录 B 清单。回滚点 `_pre_format_backup/`（含 `论文初稿.md.bak`、`.bak2`）✅。**参考文献标注**：正文补 `[1,2]`/`[2]`/`[4,5]`/`[3]`/`[6]` 共 5 处，与文末 `[1]–[6]` 呼应 ✅ | ✅ 已完成 |
| 交付校验 | ✅ **`validate_consistency.py`**（新增，13/13 全通过）：① 报告值 ↔ `result*.xlsx` ↔ `_*.txt` 汇总（问题1 购电量 59482.6989、问题2 紧急购电量 575740.84 kWh、问题3 总费 14 476 916.62 与调整费 356 531.06、问题4-3 总费 15 073 388.70）；② **5 张时段表"144 段之和 = 表末全天购电量列"，最大偏差 0.0012 kWh**（同时验证了改写后的写入函数未破坏总量守恒）；③ 论文 14 个关键数字齐全。连同 `validate_results.py`（物理不变量 38/38）构成交付级自动校验 | ✅ 已完成 |

---

## 7. 文件变更清单与回滚

**新增**
- `solve_rolling_mpc.py` —— 滚动层 A/B/C 三方案对照实验
- `analyze_cvar.py`、`analyze_degradation.py`、`analyze_load_uncertainty.py`、`analyze_robust.py` —— §6.6–6.9 的四项拓展分析
- `validate_consistency.py` —— 交付级一致性校验（13 项：报告值 ↔ result*.xlsx ↔ 汇总 txt、时段表总量守恒、论文数字出处）
- `make_docx.py` —— 一键生成 `论文初稿.docx`（pandoc + 中英文字体定制 + 自校验）
- `_rolling_mpc_summary.txt`、`_cvar_summary.txt`、`_degradation_summary.txt`、`_load_uncertainty_summary.txt`、`_robust_summary.txt`、`_consistency.txt` —— 对应汇总报告
- `_OPTIMIZATION_LOG.md` —— 本日志
- `_pre_mpc_backup/`、`_pre_slotfix_backup/`、`_pre_format_backup/` —— 三个回滚点（分别对应状态反馈改造、时段口径修正、论文格式化与页数精简）
- `_unused_figures/` —— 7 张未使用图（含 PDF 共 14 个文件：影子价格、测试 B/C、图 2/4/5、测试 A）

**修改**
- `common.py` —— 新增 `advance_actual_soc()`
- `solve_q4.py` —— `run_rolling` 改为状态反馈版；旧口径保留为 `run_rolling_forecast_driven`；更新对比数字
- `validate_results.py` —— 问题3/4 期望值更新 + 新增 3 项状态反馈递推校验
- `result3.xlsx`、`result4-3.xlsx` —— 按新口径重算
- `论文初稿.md`、`论文初稿.docx` —— 数字与机制刷新
- `_q23_continuous_summary.txt`、`_q4_summary.txt`、`_validation.txt` —— 重算/重跑产出

**未改动**
- `附件/`（含附件1–4、附件5 模板）、`C题.pdf` —— 只读输入，永不写入
- `result1.xlsx`、`result2.xlsx`、`result2_deterministic.xlsx`、`result4-2.xlsx` —— 数值未变

**回滚**：`cp _pre_mpc_backup/* .`（覆盖回改动前状态）。

---

## 8. 复现命令

```bash
python solve_rolling_mpc.py         # A/B/C 三方案对照 → _rolling_mpc_summary.txt
python solve_q23_continuous.py      # 问题2/3（附件1 同价）→ result2/result3.xlsx
python solve_q4.py                  # 问题4（波动电价）→ result4-2/result4-3.xlsx
python validate_results.py          # 物理不变量 + 状态反馈校验 → _validation.txt（38/38）
pandoc 论文初稿.md -o 论文初稿.docx   # 论文 Word 版
```

---
